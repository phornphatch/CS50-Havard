from pyfiglet import Figlet
from cs50 import get_string
from sys import argv

if len(argv) > 2:
    if argv[1] == '-f' or argv[1] == '--font'
    text = get_string("Input: ")
    print("Output: ")
    new_text = text.setFont(argv[2])
    print(new_text)
    else:
    print("Invalid usage")
elif len(argv) == 2

